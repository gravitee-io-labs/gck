# CLAUDE.md

Claude Code reads this file as project memory. Every other agent reads `AGENTS.md`.
Keep the instructions in `AGENTS.md` — this file only imports them, so there is one
source of truth rather than two that drift.

@AGENTS.md
