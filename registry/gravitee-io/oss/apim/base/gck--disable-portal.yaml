description: "Disable the developer portal UI"
components:
  - name: apim
    helm:
      values:
        portal:
          enabled: false
images:
  preload:
    skip:
      - "graviteeio/apim-portal-ui:{{ .imageTag }}"
