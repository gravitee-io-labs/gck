vars:
  clusterName:
    default: "mysql-standalone"
    description: "Name of the Kind cluster"
  imageRepository:
    default: "mysql"
    description: "MySQL Docker image repository"
  imageTag:
    default: "9"
    description: "MySQL Docker image tag"
  mysqlDatabase:
    default: "gravitee"
    description: "Database to create on first startup"
images:
  preload:
    refs:
      - "{{ .imageRepository }}:{{ .imageTag }}"
kind:
  name: "{{ .clusterName }}"
  nodes:
    - role: control-plane
      extraPortMappings:
        - containerPort: 30306
          hostPort: 30306
components:
  - name: mysql
    type: k8s
    k8s:
      manifests:
        - apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: mysql
            labels:
              app: mysql
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: mysql
            template:
              metadata:
                labels:
                  app: mysql
              spec:
                containers:
                  - name: mysql
                    image: "{{ .imageRepository }}:{{ .imageTag }}"
                    ports:
                      - containerPort: 3306
                        name: mysql
                    env:
                      - name: MYSQL_DATABASE
                        value: "{{ .mysqlDatabase }}"
                      - name: MYSQL_ROOT_PASSWORD
                        value: mysql
                    resources:
                      requests:
                        cpu: 250m
                        memory: 256Mi
                      limits:
                        cpu: "1"
                        memory: 512Mi
        - apiVersion: v1
          kind: Service
          metadata:
            name: mysql
            labels:
              app: mysql
          spec:
            type: NodePort
            ports:
              - port: 3306
                targetPort: 3306
                nodePort: 30306
                name: mysql
            selector:
              app: mysql
